<IfModule mod_rewrite.c>
    RewriteEngine on
    # Securely rewrite all incoming web requests to Laravel's public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
